# 🌌 LAZERBEAM BACKUP 3000 - .env.example
# Copy this to `.env` and adjust paths if needed

# Automatically set to your current user (usually don't change this)
USERNAME=$(whoami)

# 📂 Where to save your backups (iPhone DCIM will be copied here)
BACKUP_DIR="/var/home/$USERNAME/Pictures/iphone-lazerbackup"

# 📄 Log file path (used by both lazerbeam.sh and mini-lazer.sh)
LOG_FILE="$BACKUP_DIR/lazerbeam.log"

# 🔕 Disable bell sound? Uncomment and set to 1 if you hate fun
# DISABLE_SOUND=1